@import "~src/styles.styl";

.container {
  height: 100vh;
  background-color: bgColor;
  color: fgColor;
}

.status {
  display: flex;
  justify-content center;
}

.map {
  display: flex;
  flex-direction: column;
  align-items center;
}

.row {
  display: flex;
}
